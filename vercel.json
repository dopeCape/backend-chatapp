{
  "version": 2,
  "builds": [{ "src": "src/**/*.ts", "use": "@now/node" }],
  "routes": [{ "src": "/(.*)*", "dest": "dist/index.js" }],
  "builds": {
    "env": {
      "PRISMA_GENERATE": "npm run prisma:generate"
    },
    "scripts": {
      "build": "vercel-build"
    }
  }
}
